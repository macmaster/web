import React from "react";
import ReactDOM from "react-dom";
import Button from "material-ui/Button";

import SearchBar from "./SearchBar";
import Clock from "./Clock";
import NavButton from "./NavButton";
import Table from "./Table";

// // setup search bar
// ReactDOM.render(<SearchBar />, document.getElementById("search-bar"));
// document.getElementById("search-query").focus();

// setup clock element
ReactDOM.render(<Clock/>, document.querySelector("#clock"));

var p = document.createElement('p');
p.innerHTML = "{generated by javascript}";
p.color = "green";
document.getElementById("footer").insertBefore(p, null);

// fiddle with the DOM
// document.bgColor = "#002233";
// document.fgColor = "#FFDDCC";
  
// navigation bar
var navButtons = [
  "google.com",
  "mozilla.org",
  "nytimes.com",
  "youtube.com",
].map(domain =>
  <NavButton 
    key={domain}
    className="nav-button" 
    href={`http://${domain}`}
  >{domain}</NavButton>
);
ReactDOM.render(navButtons, document.getElementById("nav-bar"));

let productTable = ReactDOM.render(
  <Table columns={["name", "price", "category"]} />,
  document.getElementById("product-table")
);
productTable.setRows([
  { name: "chips", price: 1.05, category: "food", },
  { name: "pastry", price: 1.55, category: "food", },
  { name: "coca-cola", price: 0.75, category: "drink", },
]);
